node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Photos uploadées (CRA, etc.) — ne pas versionner.
# Le dossier reste suivi grâce à .gitkeep.
/uploads/*
!/uploads/.gitkeep

# Logs structurés (audit déploiements, etc.) — créés au runtime.
/logs
